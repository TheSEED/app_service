{
  "id": "MetagenomicGeneMapping",
  "label": "Metagenomic gene mapping",
  "script": "App-MetagenomicGeneMapping",
  "description": "Map metagenomic reads to a defined gene set",
  "parameters": [
    {
      "desc": "Input type (single end reads / paired end reads / SRA id )",
      "required": 1,
      "default": null,
      "label": "Input Type",
      "type": "enum",
      "enum": [
        "single_end_reads",
        "paired_end_reads",
        "sra_ids"
      ],
      "id": "input_type"
    },
    {
      "desc": "Gene set type (predefined list / fasta file / feature group )",
      "required": 1,
      "default": null,
      "label": "Input Type",
      "type": "enum",
      "enum": [
        "predefined_list",
        "fasta_file",
        "feature_group"
      ],
      "id": "gene_set_type"
    },
    {
      "id": "paired_end_libs",
      "type": "group",
      "allow_multiple": false,
      "required": 0,
      "default": null,
      "group": [
        {
          "id": "read1",
          "required": 1,
          "label": "Read file 1",
          "desc": "FASTQ or FASTA file of reads",
          "wstype": "ReadFile",
          "type": "wstype"
        },
        {
          "id": "read2",
          "required": 0,
          "label": "Read file 2",
          "desc": "FASTQ or FASTA file of reads",
          "wstype": "ReadFile",
          "type": "wstype"
        }
      ]
    },
    {
      "id": "single_end_libs",
      "type": "group",
      "allow_multiple": false,
      "required": 0,
      "default": null,
      "group": [
        {
          "id": "read",
          "required": 1,
          "label": "Read file",
          "desc": "FASTQ, FASTA or H5 file of reads",
          "wstype": "ReadFile",
          "type": "wstype"
        }
      ]
    },
    {
      "id": "srr_ids",
      "required": 0,
      "allow_multiple": false,
      "default": null,
      "label": "SRR ID",
      "desc": "Sequence Read Archive (SRA) Run ID",
      "type": "string"
    },
    {
      "id": "output_path",
      "label": "Output Folder",
      "required": 1,
      "default": null,
      "desc": "Path to which the output will be written. Defaults to the directory containing the input data. ",
      "type": "folder"
    },
    {
      "id": "output_file",
      "label": "File Basename",
      "required": 1,
      "default": null,
      "desc": "Basename for the generated output files. Defaults to the basename of the input data.",
      "type": "wsid"
    }
  ]
}
